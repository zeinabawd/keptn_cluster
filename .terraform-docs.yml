formatter: markdown

# Uncomment to enable terraform-docs for submodules in the ./modules folder.
# recursive:
#  enabled: true

output:
  file: README.md
  mode: inject
  template: |-
    <!-- BEGIN_TF_DOCS -->
    <!-- Content between BEGIN_TF_DOCS and END_TF_DOCS is generated by terraform-docs. -->
    <!-- Run `terraform-docs .` in the base repository directory to regenerate. -->
    {{ .Content }}
    <!-- END_TF_DOCS -->